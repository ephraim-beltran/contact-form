stages:
  - deploy

pages:
  tags:
    - node
  stage: deploy
  script:
    - npm install
    - npm run build
    - rm -rf public
    - cp dist/index.html dist/404.html
    - mv dist public
  artifacts:
    paths:
      - public

  only:
  - main
